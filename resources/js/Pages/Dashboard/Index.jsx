import Dashboard from "@/Layouts/Dashboard"

export default function Index() {

    const handleChange = (data) => {
        console.log(data);
    }

    return (
        <Dashboard>

        </Dashboard>
    )
}
